// Injected by vite.config.ts's `define` at build time (total git commit
// count) - not a real module/import, just a global constant substituted
// into the bundle, so it needs an ambient declaration rather than a type-only
// import.
declare const __GIT_VERSION__: string;
